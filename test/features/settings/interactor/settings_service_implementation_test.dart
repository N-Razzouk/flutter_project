// import 'package:ed/features/settings/interactor/settings_service_implementation.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Test settings service implementation.', () {
    test('Test if the settings service implementation is correct.', () async {
      /// Arrange
      // final settingsService = UserSettingsServiceImpl();

      // /// Act
      // final authorizationStatus =
      //     await settingsService.getAuthorizationStatus();
      // final authorizeUser = await settingsService.authorizeUser();
      // final deauthorizeUser = await settingsService.deauthorizeUser();
      // final userBalance = await settingsService.getUserBalance();
      // final depositMoney = await settingsService.depositMoney(100.0);

      /// Assert
    });
  });
}
